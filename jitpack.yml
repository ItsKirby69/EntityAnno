build:
  skip: true
jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.12-oracle
  - sdk use java 17.0.12-oracle
install:
  - ./gradlew clean -Pgroup=com.github.ItsKirby69 -Pversion=$VERSION -xtest -x javadoc assemble publishToMavenLocal